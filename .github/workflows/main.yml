name: Rust

on:
  push:
    branches: 
      - master
      - develop
  pull_request:
    branches: 
      - master
      - develop

env:
  CARGO_TERM_COLOR: always

jobs:
  unit-test:
    name: ⚙️ Unit tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
  
  examples-run:
    name: ⚗️ Run examples
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Launch Kuzzle stack
      run: docker compose up -d
    - name: Build
      run: cargo build --verbose
    - name: Run examples
      run: cargo run --example now
